{
  "name": "ip-address-tracker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^8.1.1"
  },
  "dependencies": {
    "maplibre-gl": "^5.24.0"
  }
}
